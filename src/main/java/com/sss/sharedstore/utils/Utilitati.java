package com.sss.sharedstore.utils;

public class Utilitati {

    public static double calculPondere(double vanzari, double accesari) {
        if (accesari == 0) {
            return vanzari + (accesari / 100);
        }
        return 0;
    }
}
